<%= @meeting.subject %>
<%= @meeting.date %>
<%= @meeting.time %>

Participants:

  <% @meeting.users.each  do |user| %>
      - <%= user.name %>
  <% end %>

<%= textilizable(@meeting, :agenda, :only_path => false) %>
